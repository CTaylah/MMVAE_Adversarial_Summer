# experiments.yaml
experiment_name: embedding
run_name: Embedding_Output_euclidean_w0.30
train_command:
  fit:
    # model: configs/model/3_species.yaml
    model: configs/liam_based/liam_clone_conditionals_no_adv.yaml
    trainer:
      track:
        # test: configs/trainer/config.test.yaml
        full: configs/trainer/config.yaml
    data:
      track:
        local: configs/data/local_chunk.yaml
        # server: configs/data/server.yaml

categories:
- 'dataset_id'
- 'cell_type'
- 'species'

merge_keys:
- z