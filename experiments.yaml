# experiments.yaml

experiment_name: February_4th
run_name: GRF_lamda_100
train_command:
  fit:
    model: configs/model/parallel/adversarial-conditional.yaml
    trainer:
      track:
        # test: configs/trainer/config.test.yaml
        full: configs/trainer/config.yaml
    data:
      track:
        local: configs/data/local_chunk.yaml
        # server: configs/data/server.yaml

categories:
- 'donor_id'
- 'assay'
- 'dataset_id'
- 'cell_type'
- 'species'

merge_keys:
- z
