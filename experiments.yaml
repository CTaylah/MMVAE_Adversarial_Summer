# experiments.yaml
experiment_name: liam_stuff_2
run_name: distance_loss_10
train_command:
  fit:
    model: configs/liam_based/liam_clone.yaml
    trainer:
      track:
        # test: configs/trainer/config.test.yaml
        full: configs/trainer/config.yaml
    data:
      track:
        local: configs/data/local_chunk.yaml
        # server: configs/data/server.yaml

categories:
- 'donor_id'
- 'assay'
- 'dataset_id'
- 'cell_type'
- 'species'

merge_keys:
- z